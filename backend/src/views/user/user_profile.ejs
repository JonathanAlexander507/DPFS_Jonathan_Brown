<!DOCTYPE html>
<html lang="en">
<head>
    <%- include("../partials/head") %>
</head>
<body>
    <header><%- include("../partials/header") %></header>
    <main>
        <div class="perfil_container">
            <h1>Perfil de <%= user.name %></h1>
            <div class="perfil_info">
                <img src="/images/users/profile_photo/<%= user.profile_photo %>" alt="Foto de perfil">
                <p><strong>Nombre:</strong> <%= user.name %> <%= user.last_name %></p>
                <p><strong>Correo:</strong> <%= user.email %></p>
                <p><strong>Provincia:</strong> <%= user.province %></p>
                <p><strong>Tipo de usuario:</strong> <%= user.user_type === 'administrador' ? 'Administrador' : 'Cliente' %></p>
            </div>
        </div>
    </main>
    <footer><%- include("../partials/footer") %></footer>
</body>
</html>
